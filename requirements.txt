# Layer 1 — vehicle model
numpy

# Layer 2 — track environment (FastF1 reconstruction + geometry)
scipy
fastf1

# Layer 4 — gym environment wrapper
gymnasium

# Layer 5 — SAC driver agent (torch: the CPU build is enough — on a no-GPU
# box: pip install torch --index-url https://download.pytorch.org/whl/cpu)
stable-baselines3
torch

# Test runner (tests also run standalone: python tests/test_track.py)
pytest
